{
  "identifier": "default",
  "description": "Default permissions for AniDownloader",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "notification:default",
    "fs:default",
    "shell:default",
    "opener:default"
  ]
}
